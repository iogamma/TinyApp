<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include partials/_header %>
    urls_index.ejs
  </header>

  <ul>
    <% for(url in urls) { %>
      <li><%= `${url}: ${urls[url]}` %>
      <form method="POST" action="/urls/<%= url %>/delete">
        /urls/<%= url %>/delete
        <input type="submit" value="Delete">
      </form>
    <% } %>
  </ul>



</body>
</html>
