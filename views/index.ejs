<!doctype html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body style='font-size:20px; padding:30px;'>
  <header>
    <% include partials/_header %>
  </header>
  <h2>Welcome to TinyApp!</h2>
  <b>Try it it out:</b><br><br>
  <form  method="POST" action="/" style="margin:150;">
    <label for="longURL">Enter a URL:</label>
    <input id="longURL" type="text" name="longURL" placeholder="http://" style="width: 300px">
    <input type="submit" value="Submit">
  </form>
  <br>
  <% if (`${Object.keys(tryMeURL)}` !== 'initTemp') { %>
    Your long URL is: <a href=<%= `${tryMeURL[`${Object.keys(tryMeURL)}`]}` %>><%= `${tryMeURL[`${Object.keys(tryMeURL)}`]}` %></a>
    <br>
    Your tiny URL is:
    <a href=<%= `/u/${Object.keys(tryMeURL)}` %>><%= `localhost:8080/u/${Object.keys(tryMeURL)}`%></a>
  <% } %>
  <br>
  <a href='/register'>Register</a> today to store your own tiny URLs!
</body>
</html>